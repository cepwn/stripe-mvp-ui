import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#e61955',
    },
    secondary: {
      main: '#1E1E1E',
    },
  },
});

export default theme;
