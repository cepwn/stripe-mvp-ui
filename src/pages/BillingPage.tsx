import { FC, ReactElement } from 'react';
import BillingProfile from '../components/Billing/BillingProfile';

const BillingPage: FC = (): ReactElement => {
  return <BillingProfile />;
};

export default BillingPage;
