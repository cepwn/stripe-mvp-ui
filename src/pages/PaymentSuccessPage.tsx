import { FC, ReactElement } from 'react';
import PaymentSuccess from '../components/Checkout/PaymentSuccess';

const PaymentSuccessPage: FC = (): ReactElement => {
  return <PaymentSuccess />;
};

export default PaymentSuccessPage;
