import { Typography } from '@mui/material';
import { FC, ReactElement } from 'react';

const ProductForm: FC = (): ReactElement => {
  return <Typography variant="h4">Product Form</Typography>;
};

export default ProductForm;
